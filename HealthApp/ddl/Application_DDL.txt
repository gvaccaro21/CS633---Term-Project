create table measure(MEASURE_ID INTEGER NOT NULL PRIMARY KEY,
USER_ID INTEGER NOT NULL, 
MEASURE_BMI INTEGER, 
MEASURE_HEIGHT INTEGER, 
MEASURE_WEIGHT INTEGER);

INSERT INTO measure(MEASURE_ID, USER_ID, MEASURE_BMI, MEASURE_HEIGHT, MEASURE_WEIGHT) VALUES (1, 1, 15, 30, 45);